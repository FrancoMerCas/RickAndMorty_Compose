query CharacterList($page: Int) {
    characters(
        page: $page
    ) {
        results {
            id
            name
            species
            image
        }
        info {
            count
            pages
            next
            prev
        }
    }
}